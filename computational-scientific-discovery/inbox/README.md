# Inbox

Place new, not-yet-coded literature notes here. After reading enough to assign the metadata in the [coding schema](../coding-schema.md), move the note into `papers/` and rebuild the indexes.

